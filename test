protected readCustomer(ticket: Ticket): boolean {
  if (ticket.discussionGroup?.users?.some(u => u.ticketRead)) {
    return true;
  }

  if (!ticket.readCustomer) {
    return true;
  }

  return false;
}
