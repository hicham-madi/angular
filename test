.filter-bar {
  select {
    @extend .font-size-medium;
    padding: 8px 12px;
    border: 1px solid $text-border;
    border-radius: 8px;
    color: $canard-fonce;
    width: 235px;
    height: 33px;
    outline: none;

    option:checked {
      background-color: $canard-fonce; // ou une couleur de ton fichier colors.scss
      color: $white;                   // texte en blanc pour le contraste
      font-weight: bold;               // gras uniquement pour la sélection
    }
  }
}
