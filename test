.icon-wrapper {
  position: relative; // Ajouté
  display: flex;
  align-items: center;
  width: 100%;
  height: 100%;
  cursor: pointer;
  color: $canard-clair;
  transition: color 0.2s ease;

  &:hover {
    color: $danger;
  }
}
