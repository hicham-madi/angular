<!-- Ligne contenant les tabs et le datepicker -->
<div class="uk-flex uk-flex-middle uk-flex-between uk-margin-small-bottom">
  <!-- Onglets -->
  <metabot-status-tabs
    [tabs]="statusTabs"
    (tabChange)="onTabChange($event)"
    [currentCount]="tableConfigFactory.ticketCountSolicitations"
    [selectedTab]="currentTab">
  </metabot-status-tabs>

  <!-- Date range picker affiché uniquement pour l'onglet "Terminées" -->
  <div *ngIf="currentTab === FilterCardType.TERMINATED" class="uk-margin-left">
    <mat-form-field appearance="fill" class="date-picker-terminated">
      <mat-label>Plage de dates</mat-label>
      <mat-date-range-input [rangePicker]="picker" (dateChange)="onDateRangeChange($event)">
        <input matStartDate placeholder="Début" [value]="startDate" readonly>
        <input matEndDate placeholder="Fin" [value]="endDate" readonly>
      </mat-date-range-input>
      <mat-datepicker-toggle matSuffix [for]="picker"></mat-datepicker-toggle>
      <mat-date-range-picker #picker></mat-date-range-picker>
    </mat-form-field>
  </div>
</div>

.date-picker-terminated {
  min-width: 300px;
  margin-left: 16px;
}
