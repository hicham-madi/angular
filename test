private _currentCount: number | undefined;

@Input()
set currentCount(value: number | undefined) {
  this._currentCount = value;
  setTimeout(() => this.cdr.detectChanges());
}
get currentCount(): number | undefined {
  return this._currentCount;
}
