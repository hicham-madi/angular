/* Cible uniquement le composant metabot-file-upload présent dans chat-form */
:host ::ng-deep metabot-file-upload .attachment-icon-block > button.text-button {
  border: 2px solid var(--canard-fonce);
  border-radius: 8px;
  color: var(--canard-fonce);
}

:host ::ng-deep metabot-file-upload 
  .attachment-icon-block > button.text-button 
  .material-symbols-outlined {
  color: var(--canard-fonce);
}
