if (cardTypeConfig == CardTypeConfig.REQUESTS) {
    tickets = hazelcastRepository.findMatchingTickets(
        user.getOrg(),
        filters.getCustomer().getId(),
        TicketCriteria.requests(filters.getCustomer().getId()) // <- Hazelcast Predicate OK
    );
}
