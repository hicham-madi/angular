public ngAfterViewInit(): void {
  setTimeout(() => {
    const tab: [Tab, string] | undefined = Array.from(this.tabsUrls).find(([_, path]) =>
      this.router.url.includes(path)
    );

    if (tab) {
      this.activeTab = tab[0];
      this.cdr.detectChanges();
    }
  });
}
