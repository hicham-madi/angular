<!-- En-tête -->
<metabot-page-header
  [currentCount]="tickets?.length"
  [title]="title">
</metabot-page-header>

<!-- Cartes -->
<div *ngIf="smallCards.length > 0 || largeCards.length > 0" class="dashboard-summary">
  <ng-container *ngFor="let card of smallCards">
    <metabot-dashboard-card
      class="card"
      [card]="card"
      [class]="card.variant"
      [activeType]="activeCard"
      (cardclicked)="onCardClicked($event)">
    </metabot-dashboard-card>
  </ng-container>

  <div class="dashboard-separator"></div>

  <ng-container *ngFor="let card of largeCards">
    <metabot-dashboard-card
      class="card"
      [card]="card"
      [class]="card.variant"
      [activeType]="activeCard"
      (cardclicked)="onCardClicked($event)">
    </metabot-dashboard-card>
  </ng-container>
</div>
