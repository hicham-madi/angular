readonly DEFAULT_AGENCY = { code: 'ALL', label: 'Toutes les agences' };

filteredAgencies = [
  this.DEFAULT_AGENCY,
  ...autresAgences // ton tableau habituel
];

selectedAgencyCtrl = new FormControl(this.DEFAULT_AGENCY);
