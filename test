var dtoList = Optional.ofNullable(pageDto)
        .map(MbotPageDto::getTickets)
        .map(Stream::toList)
        .orElseGet(List::of);

List<Ticket> ticketList = IMapperTicket.INSTANCE.listTicketMbotTicketDtoToListTicket(dtoList);
