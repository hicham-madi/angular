<div class="uk-flex uk-flex-middle uk-flex-between">
  <metabot-status-tabs
    [tabs]="statusTabs"
    (tabChange)="onTabChange($event)"
    [currentCount]="tableConfigFactory.ticketsCountSolicitations"
    [selectedTab]="currentTab"
  ></metabot-status-tabs>

  <!-- Rangepicker : Filtre de période -->
  <ticketing-form-field-details-period
    [readOnly]="true"
    [field]="{
      key: 'daterangepicker',
      type: 'daterangepicker',
      props: {
        label: 'Sélecteur de période',
        daterangepickerOptions: { startView: 'month' }
      }
    }"
    [displayOrValidationRuleFields]="[]"
  ></ticketing-form-field-details-period>
</div>

public dateRangeField: Field = {
  key: 'daterangepicker',
  type: 'daterangepicker',
  props: {
    label: 'Sélecteur de période',
    daterangepickerOptions: {
      startView: 'month'
    }
  }
};
