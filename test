String trimmed = query.trim();
String sub = trimmed.length() > 11 ? trimmed.substring(0, 11) : trimmed;
String format = String.format("%s", sub);

case "FORM_VALUES":
    return Criteria.where(FORMS_FIELD)
                   .elemMatch(Criteria.where("fValue").regex("^" + Pattern.quote(format)));
