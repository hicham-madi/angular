.status-tabs {
  .tab {
    border-top-left-radius: 6px;
    border-top-right-radius: 6px;
    cursor: pointer;
    background-color: $vert-prairie-clair; // facultatif, couleur de base

    &:hover {
      background-color: $vert-prairie;
    }

    &.active {
      color: $canard-fonce;
      border-bottom: 2px solid transparent;
      background-color: $vert-prairie; // actif + hover = même fond
    }

    &.inactive {
      background-color: $vert-prairie-clair;
    }
  }
}

[class.hovering]="hoveringTabs"
(mouseover)="hoveringTabs = true"
(mouseleave)="hoveringTabs = false"
