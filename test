/* Conteneur du champ */
.status {
  position: relative;
  display: inline-block;
  width: 250px; /* ajuste selon ta maquette */
}

/* Le select styled */
.status select {
  width: 100%;
  padding: 10px;
  border: 2px solid $text-border; /* utilise ta variable SCSS */
  border-radius: 6px;
  font-size: 14px;
  background: #fff;
  appearance: none; /* supprime la flèche par défaut */
}

/* Label encapsulé dans la bordure */
.status label {
  position: absolute;
  top: -10px; /* remonte dans la bordure */
  left: 10px;
  background: #fff; /* blanc pour masquer le fond */
  padding: 0 4px;
  font-size: 12px;
  color: $text-border; /* harmonisé avec la bordure */
}

<div class="status">
  <label for="searchType">Type de recherche</label>
  <select id="searchType"
          [(ngModel)]="selectedSearchType"
          (change)="onSearchTypeChange()">
    <option *ngFor="let searchType of searchTypes" [value]="searchType">
      {{ searchType }}
    </option>
  </select>
</div>
