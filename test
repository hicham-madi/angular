public static Predicate<Object, Ticket> readCustomer(String userId) {
    return Predicates.or(
        requestsReadCustomer(userId),
        claimsReadCustomer(userId),
        discussionGroupsReadCustomer(userId),
        requestsExpertServiceReadCustomer(userId)
    );
}
