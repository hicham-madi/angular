<cdk-virtual-scroll-viewport
  [itemSize]="rowHeight"
  class="table-viewport"
  (scrolledIndexChange)="onCdkScroll()">

  <table #tableEl mat-table [dataSource]="tickets" class="mat-elevation-z8 h-100">
    <!-- header + rows -->
  </table>

  <!-- SPACER pour garantir la visibilité du dernier ticket -->
  <div class="scroll-tail" [style.height.px]="rowHeight"></div>
</cdk-virtual-scroll-viewport>

.scroll-tail { flex: 0 0 auto; }

:host ::ng-deep .cdk-virtual-scroll-content-wrapper {
  padding-bottom: 56px; /* ou [rowHeight] si tu le bindes en inline */
}
