periodeTermineeField = {
  key: 'periodeTerminee',
  type: 'daterangepicker',
  props: {
    label: 'Période',
    daterangepickerOptions: {
      startView: 'month'
    }
  },
  dataType: 'DateRange', // ou DataType.DateRange si tu as l'import
  value: null
};

<div class="uk-flex uk-flex-between uk-flex-middle">
  <metabot-status-tabs
    [tabs]="statusTabs"
    (tabChange)="onTabChange($event)"
    [currentCount]="tableConfigFactory.ticketsCountSolicitations"
    [selectedTab]="currentTab"
  ></metabot-status-tabs>

  <ticketing-form-field-details-period
    [field]="periodeTermineeField"
    [readOnly]="false"
    class="uk-margin-left"
  ></ticketing-form-field-details-period>
</div>

