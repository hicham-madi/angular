<mat-icon
  fontIcon="mark_chat_unread"
  class="material-symbols-outlined font-size-xxl margin-xsmall-top font-weight-medium"
  [matTooltip]="!isReadCustomer(row) ? 'marquer comme non lu' : ''"
  [matTooltipDisabled]="isReadCustomer(row)"
  matTooltipPosition="above"
  [ngClass]="{ 'danger-color': !isReadCustomer(row) }">
</mat-icon>
