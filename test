if (this.timelineRep?.dayInBetweenDates != null) {
  this.bufferValue = Math.abs(this.timelineRep.dayInBetweenDates);
} else {
  this.timelineRep = this.timeLine.find((t) => t.shortLabel === 'end') ?? undefined;
  this.bufferValue = this.treatmentDelay ?? 0;
}
