ngOnInit(): void {
  const matchedEntry = Array.from(this.tabsUrls).find(
    ([_, url]) => this.router.url.startsWith(url)
  );

  if (matchedEntry) {
    this.activeTab = matchedEntry[0];
  }
}

this.activeTab = matchedEntry?.[0] ?? Tab.SOLICITING;
