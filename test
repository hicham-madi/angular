<input-control
  class="search-control"
  [inputIcon]="{icon: searchTextControl.value ? 'close' : 'search', position: 'LEFT'}"
  [classes]="searchTextControl.value ? ['danger-color'] : []"
  [tooltip]="searchTextControl.value ? 'Réinitialiser la recherche' : ''"
  (keyup.enter)="searchType()"   <!-- ICI -->
  onClick="createSearchTextControlEmitter()"
  [labelPlaceholder]="'Rechercher...'"
  [control]="searchTextControl">
</input-control>
