setTimeout(() => {
  const elements = document.querySelectorAll('.barre');
  elements.forEach(el => {
    // Forcer le reflow en accédant à offsetHeight
    void (el as HTMLElement).offsetHeight;
  });

  this.cdkVirtualScroll?.checkViewportSize();
  this.cdr.detectChanges();
}, 50);
