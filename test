:host ::ng-deep input-control.search-control .input-wrapper {
  /* adapte au markup interne d'input-control (ex: .input-wrapper, .field, etc.) */
  height: 36px;
  border-radius: 8px;
}

:host ::ng-deep input-control.search-control input {
  padding: 0 12px;
  line-height: 36px;
}
