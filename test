<div class="status field">
  <label for="searchType">Type de recherche</label>

  <select id="searchType"
          class="field-select"
          [(ngModel)]="selectedSearchType"
          (change)="onSearchTypeChange()">
    <option *ngFor="let searchType of searchTypes" [value]="searchType">
      {{ searchType }}
    </option>
  </select>
</div>


/* Conteneur "input-like" avec label + select dedans */
.field {
  display: inline-flex;
  align-items: center;
  gap: 8px;                 /* espace entre le label et la valeur */
  padding: 10px 12px;       /* padding global de la “case” */
  border: 2px solid $text-border;
  border-radius: 6px;
  background: #fff;
  min-width: 280px;         /* ajuste selon la maquette */
}

/* Le label à l’intérieur de la bordure */
.field > label {
  font-size: 12px;
  color: $text-border;      /* gris doux comme sur la maquette */
  white-space: nowrap;
  margin: 0;                /* annule marges par défaut */
}

/* Le select sans bordure, intégré dans la case */
.field-select {
  flex: 1 1 auto;
  border: none;
  outline: none;
  background: transparent;
  font-size: 14px;
  padding: 0;               /* on laisse le padding au conteneur */
  appearance: auto;         /* garde la flèche native */
}

/* Focus/hover (optionnel – style Material-ish) */
.field:focus-within {
  border-color: $canard-moyen;
}
