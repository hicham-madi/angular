
protected getLabelClasses(): string[] {
  const classes = [];

  const label = this.getLabel();
  const isFinished = label.includes(this.textFinish) || this.countDay <= 0;

  if (isFinished) {
    classes.push('bar-label--white');
  }

  return classes;
}

<div [ngClass]="getLabelClasses()" class="bar-label">
  {{ getLabel() }}
</div>

.bar-label--white {
  color: white;
}

refactor: suppression de getStyle et remplacement par getLabelClasses avec ngClass
