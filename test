<div class="object-soliciting"
     [ngClass]="{ 'font-weight-medium': !readCustomer(row) }"
     [tooltip]="getDomainTooltip(row)"
     tooltipHtml="true"
     tooltipClass="tooltip-multiline"
     appendToBody="false"
     placement="right-start"
     triggers="hover"
     show-delay="150"
     hide-delay="100">
  {{ row2.object?.label }}
</div>

.tooltip-multiline {
  white-space: pre-line; // Respecte les retours à la ligne
  max-width: 300px; // Évite les tooltips trop larges
  text-align: left;
}

getDomainTooltip(row: any): string {
  return `000000_DS_TEST DS INTERNE\nDeuxième ligne\nTroisième ligne`;
}
