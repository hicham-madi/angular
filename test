List<Ticket> ticketList = Optional.ofNullable(pageDto)
        .map(MbotPageDto::getTickets)
        .map(Stream::toList)
        .filter(list -> !list.isEmpty())
        .map(IMapperTicket.INSTANCE::listTicketMbotTicketDtoToListTicket)
        .orElseGet(List::of);
