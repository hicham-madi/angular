Set<Ticket> uniqueTickets = new HashSet<>(hazelcastRepository.findMatchingTickets(
    customer.getOrg(),
    filters.getCustomer().getId(),
    filters.getCustomer().getEdsId(),
    mbotRequestData.getContext()
));
List<Ticket> tickets = new ArrayList<>(uniqueTickets);
