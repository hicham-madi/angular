this.largeCards = cards.largeCards.filter(card => {
  if (!this.enableReclamation && card.type === 'CLAIMS') {
    return false;
  }
  if (!this.enableDs && card.type === 'CUSTOMER_EDS_DS') {
    return false;
  }
  return true;
});
