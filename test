protected getDsTreatmentDate(row: Ticket): Date | null {
  if (!row?.ds?.timeline) return null;
  const endItem = row.ds.timeline.find(t => t.shortLabel === 'end');
  return endItem?.treatmentDate ?? null;
}
    this.ticketService.clearOpenedFromTab(); // nettoyage
  }
}
