db.getCollection("tickets_CR88B2").aggregate([
  {
    $group: {
      _id: "$claimType.label",   // distinct sur label
      ids: { $addToSet: "$_id" } // regroupe les ids liés à chaque label
    }
  }
])
