return forkJoin({
  env: this.mbotService.getEnvConfig().pipe(take(1)),
  tickets: this.mbotService.getTicketListV2(params).pipe(take(1)),
}).pipe(
  map(({ env, tickets }) => ({
    ...base,
    env,
    tickets,
  } satisfies TicketListPrefetch))
);
